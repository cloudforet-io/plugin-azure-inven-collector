---
cloud_service_group: Database
cloud_service_type: SQLServer
name: SQLServer Total Count per Subscription
query:
  aggregate:
    - group:
        keys:
          - name: account
            key: account
        fields:
          - name: value
            key: account
            operator: count
options:
  chart_type: DONUT
