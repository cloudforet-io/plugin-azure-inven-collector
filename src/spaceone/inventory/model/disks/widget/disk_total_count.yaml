---
cloud_service_group: Disks
cloud_service_type: Disk
name: Total Count
query:
  aggregate:
    - group:
        fields:
          - name: value
            operator: count
options:
  value_options:
    key: value
    type: count
    options:
      default: 0
