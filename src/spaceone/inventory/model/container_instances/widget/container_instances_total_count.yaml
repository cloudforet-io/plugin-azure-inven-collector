---
cloud_service_group: ContainerInstances
cloud_service_type: Container
name: Total Count
query:
  aggregate:
    - group:
        fields:
          - name: value
            operator: count
options:
  value_options:
    key: value
    type: text
    options:
      default: 0
