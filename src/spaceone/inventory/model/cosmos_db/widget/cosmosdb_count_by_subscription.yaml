---
cloud_service_group: CosmosDB
cloud_service_type: Instance
name: AzureCosmosDB Total Count by Subscription
query:
  aggregate:
    - group:
        keys:
          - name: account
            key: account
        fields:
          - name: value
            key: account
            operator: count
options:
  chart_type: DONUT
